apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus.monitoring.svc:9090
    isDefault: true
    editable: false
    version: 1
    # basicAuth: false
    # With TLS in front of Prometheus, you can add:
    # tlsSkipVerify: true
